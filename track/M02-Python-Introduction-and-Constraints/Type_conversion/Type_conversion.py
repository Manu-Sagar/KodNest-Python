age_text="21"
course_fee_text="499.50"
attempts=3

age=int(age_text)
course_fee_text=float(course_fee_text)
attempts_text=str(attempts)

print(age,type(age))
print(course_fee_text,type(course_fee_text))
print(attempts_text,type(attempts_text))